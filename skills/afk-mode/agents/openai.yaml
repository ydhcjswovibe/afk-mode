interface:
  display_name: "AFK Mode"
  short_description: "Budgeted repo automation with branch isolation"
  default_prompt: "Use $afk-mode <budget> as the public entrypoint for bounded repo automation. Treat begin-run and advance-run as internal helpers, not user-facing steps."

policy:
  allow_implicit_invocation: false
